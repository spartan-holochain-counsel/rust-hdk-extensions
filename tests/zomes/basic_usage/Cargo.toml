[package]
name = "basic_usage"
version = "0.0.0"
edition = "2018"

[lib]
name = "basic_usage"
crate-type = ["cdylib", "rlib"]

[dependencies]
serde = "1"
test_types = { path = "../../test_types" }
whi_hdk_extensions = { path = "../../.." }
